language: haskell
env: PATH = "~/.cabal/bin:$PATH"
before_install:
  - cabal update
  - cabal install cabal-install-1.16.0.2
  - cabal --version
  - travis/update_ghc
install:
  - ~/.cabal/bin/cabal install --only-dependencies -j
